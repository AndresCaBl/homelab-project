services:
  gluetun:
    env_file:
      - ../_shared/.env
      - ./.env
    image: qmcgaw/gluetun:latest
    container_name: gluetun
    restart: unless-stopped
    cap_add:
      - NET_ADMIN
    devices:
      - /dev/net/tun:/dev/net/tun
    environment:
      - VPN_SERVICE_PROVIDER=${VPN_SERVICE_PROVIDER}
      - VPN_TYPE=${VPN_TYPE}
      - OPENVPN_USER=${OPENVPN_USER}
      - OPENVPN_PASSWORD=${OPENVPN_PASSWORD}
      - SERVER_REGIONS=${SERVER_REGIONS}
      # Safety defaults
      - FIREWALL=on            # block non‑VPN traffic (kill switch)
      - DOT=on                 # DNS-over-TLS inside the tunnel
      - VPN_PORT_FORWARDING=on
      - VPN_PORT_FORWARDING_UP_COMMAND=/bin/sh -c 'wget -O- --retry-connrefused --post-data "json={\"listen_port\":{{PORTS}}}" http://127.0.0.1:8080/api/v2/app/setPreferences 2>&1'

    volumes:
      - /srv/config/gluetun:/gluetun
    networks:
      - homelab
    healthcheck:
      test: ["CMD", "/gluetun-entrypoint", "healthcheck"]
      interval: 30s
      timeout: 10s
      retries: 5
    ports:
      - "${QBITTORRENT_WEBUI_PORT}:8080"  # qBit WebUI via gluetun
      #- "${QBITTORRENT_INCOMING_PORT}:${QBITTORRENT_INCOMING_PORT}/tcp"  # incoming peers
      #- "${QBITTORRENT_INCOMING_PORT}:${QBITTORRENT_INCOMING_PORT}/udp"
  qbittorrent:
    env_file:
      - ../_shared/.env
      - ./.env
    image: lscr.io/linuxserver/qbittorrent:latest
    container_name: qbittorrent
    restart: unless-stopped
    # Share Gluetun's network stack = kill switch + no leaks
    network_mode: "service:gluetun"
    depends_on:
      gluetun:
        condition: service_healthy
    environment:
      - WEBUI_PORT=8080
    volumes:
      - /srv/config/qbittorrent:/config
      - ${CACHE_ROOT}/downloads:/downloads
networks:
  homelab:
    external: true
