# Home Lab Network Plan (canonical)
metadata:
  version: 2t
  updated: 2025-08-13
  owner: "andrescabl"

lan:
  cidr: 192.168.4.0/22
  gateway: 192.168.4.1
  dhcp_pool:
    start: 192.168.4.50
    end: 192.168.4.200
  dns_servers:
    - 1.1.1.1
    - 8.8.8.8
  features:
    upnp: false
    ipv6: false
    wifi_used: false   # Ethernet-only for lab hosts

reservations:
  - hostname: srv-media
    role: "Ubuntu Server media/automation"
    mac_eth: "00:2B:67:C5:B8:75"
    ip: 192.168.4.90
  - hostname: lab-proxmox
    role: "Proxmox VE host"
    mac_eth: "00:2B:67:C5:BC:15"
    ip: 192.168.4.91

notes:
  - "Eero: set DHCP reservations for Ethernet MACs only."
  - "Disable UPnP and IPv6 in Eero for Phase 1."
  - "Wi‑Fi disabled on lab hosts; revisit later if needed."

# Future (placeholder for later phases)
future:
  vlans: []           # e.g., mgmt/media/guest when you add a managed switch
  services_dns: []    # e.g., move to Pi-hole later
  static_routes: []   # if you add Tailscale subnet routes or lab segments
