# === Homelab Shared Env ===
# Map containers to your Linux user so container-writes land as andres:andres
PUID=1000
PGID=1000
TZ=Australia/Brisbane

# Canonical host paths (keep these stable to make migration easy)
CONFIG_ROOT=/srv/config
MEDIA_ROOT=/srv/media
CACHE_ROOT=/srv/cache

# Optional tuning (jellyfin transcodes, qBittorrent paths)
JELLYFIN_TRANSCODES=${CACHE_ROOT}/transcodes
DOWNLOADS_INCOMPLETE=${CACHE_ROOT}/downloads/incomplete
DOWNLOADS_COMPLETE=${CACHE_ROOT}/downloads/complete
DOWNLOADS_WATCH=${CACHE_ROOT}/downloads/watch
